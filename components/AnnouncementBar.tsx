"use client";

import React from "react";

const AnnouncementBar: React.FC = () => {
  return (
    <div className="bg-primary text-on-primary text-center font-label-caps text-[11px] tracking-[0.15em] py-2 uppercase w-full">
      ARCHITECTURAL INTEGRITY IN EVERY STITCH
    </div>
  );
};

export default AnnouncementBar;
